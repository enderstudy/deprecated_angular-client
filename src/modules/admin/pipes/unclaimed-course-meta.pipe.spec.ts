import { UnclaimedCourseMetaPipe } from './unclaimed-course-meta.pipe';

describe('UnclaimedCourseMetaPipe', () => {
  it('create an instance', () => {
    const pipe = new UnclaimedCourseMetaPipe();
    expect(pipe).toBeTruthy();
  });
});
