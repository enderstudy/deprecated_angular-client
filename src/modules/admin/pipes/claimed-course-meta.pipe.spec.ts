import { ClaimedCourseMetaPipe } from './claimed-course-meta.pipe';

describe('ClaimedCourseMetaPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimedCourseMetaPipe();
    expect(pipe).toBeTruthy();
  });
});
